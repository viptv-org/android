# Android acceptance review — 2026-09-12

Reviewed immutable diff `30aef47...3c78b85` with separate Standards and Spec agents. These findings describe that diff; later fixes require their own validation. Normative source: design's frozen Roku extraction, clarified in `95fdbdd` after checking actual `PresentationScene` and `ContinuationScene` code.

## Standards

Two documented-rule findings:

1. Hero-primary and shelf-card holds share an overly broad predicate. The Roku hero primary permits source holds for new movies/episodes, after queue interception; ordinary shelf cards do not. Eligible logical row-0 cards and hero actions instead open queue Manage. Split these surfaces instead of using a general non-live Home predicate. The initial review relied on a conflicting short design table; the source-checked clarification resolves that conflict without changing Roku.
2. Added hold-only actions lack a discoverable Menu/Info/overflow equivalent. The platform-equivalence contract requires the same source/management actions through an accessible alternative.

Optional judgement: `queueStatus` is a finite wire state represented by scattered string literals (possible Primitive Obsession). This is not a release blocker or permission to refactor unrelated code.

## Spec

Four findings:

1. Home queued-next leaves the route at Home, where Back was disabled, making its cancellation branch unreachable. Back availability must include active queued-next intent.
2. Ordinary Home shelf cards receive a special Choose source hold, while eligible row-0 queue cards can bypass Manage. The corrected hero/card distinction above governs this; retain the legitimate new-movie hero hold.
3. Remove and Undo launch unversioned Home refreshes. A delayed pre-Undo response can overwrite restored rows. Only the current refresh generation may publish shelves.
4. Changing the sole hero image model can blank its artwork during a cache miss. Retain decoded artwork until the requested image is ready, as required by the visual state contract.

Guide empty-filter routing and player-menu Back ownership looked aligned by inspection. That is not a physical device pass. The TV acceptance record remains in TESTING.md.

Counts: Standards — two documented-rule findings, plus one optional smell; Spec — four findings. The strongest Standards concern is incorrect hold scope; the strongest Spec concern is unreachable queued-next cancellation.
